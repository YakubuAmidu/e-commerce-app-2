import React from "react";
import { View, Text } from "react-native";

const ProductCard = ({ stock, name, price, id, image, addToCartHandler }) => {
  return (
    <View>
      <Text>ProductCard</Text>
    </View>
  );
};

export default ProductCard;
